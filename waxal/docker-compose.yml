services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/uploads:/app/uploads
      - waxal-db:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/waxal.db

volumes:
  waxal-db:
